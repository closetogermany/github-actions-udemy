name: section 3 action 2 - node
on: push
jobs:
  validate_code:
    runs-on: ubuntu-latest
    steps:
      - name: exploratory functions
        run: pwd; |
          ls -l;
          echo "${USER}";


      # - name: clone repository to runner
      #   uses: actions/checkout@v3
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: 18
      # - run: npm i
      # - run: npm test